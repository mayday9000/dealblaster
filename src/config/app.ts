/**
 * Application configuration
 * 
 * PUBLIC_BASE_URL: Set this to your published domain (e.g., https://your-app.lovable.app)
 * to generate shareable property links that work for anyone.
 * 
 * Leave empty to use the current window origin (useful for local development).
 * 
 * IMPORTANT: After deploying your app, update this value to your production domain
 * to ensure property share links work correctly for public viewers.
 */
export const PUBLIC_BASE_URL: string = '';
